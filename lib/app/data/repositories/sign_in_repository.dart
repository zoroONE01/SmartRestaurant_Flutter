import 'package:smart_restaurant_flutter/app/data/models/account.dart';


class SignInRepository {
  // late SmartRestaurantApiHelper apiHelper;
  // late HiveBoxHelper boxHelper;
  //
  // SignInRepository({required this.boxHelper, required this.apiHelper});
  //
  // Future<void> sumbit(String username, String password) async {
  //   final model = SignInModel(username, password);
  //   final response = await apiHelper.signIn(model.toJson());
  //   if (response!['statusCode'] == 401) {
  //
  //     final refreshToken = await apiHelper.refreshToken(response["refresh_token"]);
  //   }
  // }
}
